import React from 'react';

const ClassPage = () => {
    return (
        <div>
            <button onClick={()=>window.open('/home','_blank')}>click</button>
        </div>
    );
};

export default ClassPage;